# imports
# import our_package
import pandas
import numpy as np

def test_data_classify():
    """Test that classification results are as expected based on a small subset of the data"""
    # import the small test data set
    # data =
    results = data_classify(data, params)
    # test that the number of data points classified in each class is correct